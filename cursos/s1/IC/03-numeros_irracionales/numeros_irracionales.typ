= Irracionales

Hay numero que no son racionales, numero que tiene expresión decimal infinita que no son periódicos puros ni periódicos mixtos, no son racionales $QQ$, son Irracionales $II$.

$ QQ inter II = emptyset $

El conjunto de los números racionales es denso "Entre dos números racionales existe un numero racional"

#image("./img1.png")

#align(right, quote(
  block: true,
)[Las macices cuadradas de los números primos son irracionales])

== Ejemplos

#list[
  Ejemplo:
  $plus.minus sqrt(2), plus.minus sqrt(3), dots, plus.minus e, plus.minus pi$
][
  Ejemplo: Demostrar que $sqrt(2)$ es un numero irracional

  Demostración:

  Supongamos que $sqrt(2)$ es racional, luego $sqrt(2)=a / b$ con $a,b$ co-primos. Ahora:
  $
    (sqrt(2))^2 = (a / b)^2 ; quad 2= a^2 / b^2; quad 2b^2=a^2
  $
  Por lo tanto $a^2$ es par, entonces $a$ también es par, siendo $a=2k$ con $k in ZZ$. Remplazando:
  $
    2b^2 = (2k)^2; quad 2b^2 = 4k^2; quad b^2=2k^2
  $
  Por lo tanto $b^2$ es par, entonces $b$ también es par, lo que contradice que $a,b$ son co-primos

  $therefore sqrt(2) in II$
]

#pagebreak()
#include "ejercicios.typ"
